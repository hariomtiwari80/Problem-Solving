class Solution {
    public int numIdenticalPairs(int[] arr) {
        int c=0;
        HashMap<Integer,Integer> map=new HashMap<>();
        for(int i=0;i<arr.length;i++){
            if(map.containsKey(arr[i])) c+=map.get(arr[i]);
            map.put(arr[i],map.getOrDefault(arr[i],0)+1);
        }
        return c;
    }
}
